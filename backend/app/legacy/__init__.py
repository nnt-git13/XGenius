"""
Legacy Flask app components.

The primary backend is FastAPI (see `app/main.py`). This package contains the
Flask-only models/helpers so they don't conflict with the FastAPI ORM models in
`app/models/`.
"""


