# Minimal requirements for Vercel deployment
# Heavy ML/RL dependencies removed to stay under 250MB limit

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.32
alembic==1.13.2

# CORS & Security
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Essential ML (lightweight)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2

# Optimization (required for optimizer)
ortools==9.10.4067

# LLM / AI - Multiple providers
openai==1.12.0
groq==0.11.0
google-generativeai==0.8.3

# HTTP & Caching
httpx==0.27.0
requests==2.32.3
cachetools==5.3.3

# Utilities
python-dotenv==1.0.1
click==8.1.7
python-dateutil==2.9.0

# Monitoring & Rate Limiting
slowapi==0.1.9
structlog==24.1.0

# Removed heavy dependencies for Vercel:
# - torch (PyTorch) - ~500MB+
# - stable-baselines3 - ~200MB+
# - xgboost - ~100MB+
# - scipy - ~50MB (can add if needed)
# - gymnasium - ~100MB+
# - langchain - ~50MB+
# - celery, redis - not needed for serverless
# - APScheduler - not needed for serverless

