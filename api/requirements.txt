# Minimal requirements for Vercel deployment
# Heavy ML/RL dependencies removed to stay under 250MB limit
# Based on: https://vercel.com/kb/guide/troubleshooting-function-250mb-limit

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.32
alembic==1.13.2

# CORS & Security
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Essential ML (lightweight) - REMOVED to stay under 250MB
# numpy and pandas are large (~50-80MB combined), making them optional
# The neural predictor will use pure Python fallbacks when these are not available
# numpy==1.26.4  # ~30MB - removed for Vercel
# pandas==2.2.2  # ~50MB - removed for Vercel
# scikit-learn is large (~100MB with scipy), making it optional
# The neural predictor can fall back to heuristic predictions without sklearn
# scikit-learn==1.5.1  # Would require scipy (~50MB), removed to save space
# joblib==1.4.2
# scipy is a dependency of scikit-learn (~50MB)

# Optimization (required for optimizer endpoint)
# OR-Tools is large (~80MB), removed to stay under 250MB limit
# Optimizer will use a greedy fallback algorithm instead
# ortools==9.10.4067

# LLM / AI - Multiple providers (required for copilot)
openai==1.12.0
groq==0.11.0
google-generativeai==0.8.3

# HTTP & Caching
httpx==0.27.0
requests==2.32.3
cachetools==5.3.3

# Utilities
python-dotenv==1.0.1
click==8.1.7
python-dateutil==2.9.0

# Monitoring & Rate Limiting
slowapi==0.1.9
structlog==24.1.0

# Removed heavy dependencies for Vercel (to stay under 250MB):
# - torch (PyTorch) - ~500MB+ - NOT needed for API
# - stable-baselines3 - ~200MB+ - NOT needed for API
# - xgboost - ~100MB+ - NOT needed for API
# - scipy - ~50MB - can add if needed but trying to minimize
# - gymnasium - ~100MB+ - NOT needed for API
# - langchain - ~50MB+ - NOT needed (using direct API calls)
# - celery, redis - NOT needed for serverless
# - APScheduler - NOT needed for serverless
# - prometheus-client - NOT needed for basic deployment

